[flake8]
# Игнорируем определённые правила:
ignore = 
    W503
    F811

# Исключаем папки из проверки:
exclude = 
    tests/,
    */migrations/,
    venv/,
    */venv/,
    env/,
    */env/

# Исключаем конкретные файлы:
per-file-ignores =
    */settings.py:E501
